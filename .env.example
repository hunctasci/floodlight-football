# Copy to .env for `docker compose up`. Never commit real secrets.
# Production: set a strong POSTGRES_PASSWORD and your public CLIENT_ORIGIN/DOMAIN.
POSTGRES_PASSWORD=change-me-in-production
CLIENT_ORIGIN=*
# DOMAIN=football.example.com docker compose up -d  (TLS automatic; localhost by default)
# DOMAIN=localhost
